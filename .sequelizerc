const path = require('path');

module.exports = {
  config: path.resolve('orm.config.mjs'),
  'seeders-path': path.resolve('database', 'seeds'),
  'migrations-path': path.resolve('database', 'migrations'),
};
